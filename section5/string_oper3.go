package main

import "fmt"
import "string"

func main(){
// 예제1 결합 (일반연산)

str1 := "안녕" + "나는 밥돌이야" + "근데 오늘은 햄버거 먹어야지"
str2 := "정말행복한 삶이군"
// 스트링은 저장이되면 메모리에서 변경이 안되서 새로 만들거나 없애는 등의 "버퍼"를 사용하는 경우가 많음
fmt.Println(str1+str2)

// 예제2 결합 (join)
// 다만, 고랭은 조인 연산을 하는 것이 더 포포먼스에 좋음
strSet:=  []String{}
strSet = append(sttSet, str1)
strSet = append(sttSet, str2)

}